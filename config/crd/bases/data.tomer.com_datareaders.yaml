---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: datareaders.data.tomer.com
spec:
  group: data.tomer.com
  names:
    kind: Datareader
    listKind: DatareaderList
    plural: datareaders
    singular: datareader
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Datareader is the Schema for the datareaders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatareaderSpec defines the desired state of Datareader
            properties:
              image:
                type: string
            required:
            - image
            type: object
          status:
            description: DatareaderStatus defines the observed state of Datareader
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
